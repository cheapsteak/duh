# Implementation plans are not checked in

The markdown plans in this directory are implementation detail. They exist to
guide the work while it's being done — once the code is written, the plan has
served its purpose. Checking them in bogs down the repo with stale scaffolding
that no longer matches the code and that nobody reads.

`*.md` here is gitignored (except this file). Write plans freely; they stay
local.
